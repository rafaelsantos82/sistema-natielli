package middleware

import platformlogger "espaco-terapia-os/backend/internal/platform/logger"

// RequestIDHeader is the canonical correlation header (re-export).
const RequestIDHeader = platformlogger.RequestIDHeader
